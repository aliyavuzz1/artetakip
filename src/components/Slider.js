import React from 'react'
import { Dimensions, View } from 'react-native'


const Slider = ({item, onPress, backgroundColor, textColor}) =>{
    const dimensions = Dimensions.get();
    console.log(dimensions);
    return(
    <View style={{width:'90%'}}>

    </View>
    )
}
export default Slider;