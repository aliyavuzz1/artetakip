import React from 'react';
import {
    SafeAreaView,
    ScrollView,
    StyleSheet,
    Text,
    TextInput,
    useColorScheme,
    View,
  } from 'react-native';

export const defaultStyle = StyleSheet.create({
    darkBg : {backgroundColor:'black' , color:'white'}
})